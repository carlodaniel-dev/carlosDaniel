---
const { class: className, id } = Astro.props;
---

<section
    id={id}
    data-section={id}
    class={`section ${className} w-full mx-auto px-4 py-0 scroll-m-20 sm:px-6 lg:max-w-6xl`}
>
    <slot />
</section>