---
const { class: className, id } = Astro.props;
---

<section
    id={id}
    data-section={id}
    class={`section w-full scroll-mt-28 px-2 py-0 sm:px-4 ${className ?? ""}`}
>
    <slot />
</section>