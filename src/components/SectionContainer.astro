---
const { class: className, id } = Astro.props;
---

<section
    id={id}
    data-section={id}
    class={`section ${className} py-0 px-2 sm:px-4 scroll-m-20 w-full mx-auto lg:max-w-4xl md:max-w-2xl`}
>
    <slot />
</section>